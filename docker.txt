FROM node:18-alpine
WORKDIR /app

# copy package files first for caching
COPY package*.json ./
RUN npm install --production

# application source
COPY . .

ENV PORT=3000
EXPOSE 3000

CMD ["node", "index.js"]